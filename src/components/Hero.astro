---
import { Image } from 'astro:assets';
import portrait from '../assets/portrait.webp';
import Icon from './ui/Icon.astro';
import Pill from './ui/Pill.astro';
import type { Dict } from '../i18n';
import { socials } from '../data/socials';

interface Props { t: Dict }
const { t } = Astro.props;

// Ángulo y delay de cada chispa del anillo (pares 0s, impares 0.035s).
const sparkAngles = [0, 45, 90, 135, 180, 225, 270, 315];
---

<section id="top" class="hero">
  <figure class="hero__figure">
    <span class="hero__portrait-frame">
      <button type="button" class="hero__portrait-btn" data-portrait aria-label={t.photoAlt}>
        <Image
          src={portrait}
          alt=""
          width={168}
          height={168}
          decoding="async"
          fetchpriority="high"
          class="hero__portrait"
        />
      </button>
      <span class="hero__sparks" data-sparks aria-hidden="true">
        {sparkAngles.map((deg, i) => (
          <span class="hero__spark" style={`--rot:${deg}deg;--delay:${i % 2 === 0 ? 0 : 0.035}s`}></span>
        ))}
      </span>
      <span class="hero__status" aria-label={t.statusAria} title={t.statusTitle}>😺</span>
    </span>
    <figcaption class="hero__caption">{t.role}</figcaption>
    <span class="hero__pills hero__pills--wide">
      <Pill as="span" icon="android" iconSize="14px" class="hero__brand" aria-label="Android" />
      <Pill as="span" icon="ios" iconSize="14px" class="hero__brand" aria-label="Apple" />
      <Pill as="span" icon="flutter" iconSize="15px" class="hero__brand" aria-label="Flutter" />
    </span>
  </figure>

  <div class="hero__greet">
    <span class="hero__dot"></span>
    <span class="hero__greet-text">{t.greetA}<em class="hero__em">Leo Mogiano</em>{t.greetB} <span class="hero__wave">👋</span></span>
    <span class="hero__pills hero__pills--narrow">
      <Pill as="span" icon="android" iconSize="17px" class="hero__brand" aria-label="Android" />
      <Pill as="span" icon="ios" iconSize="17px" class="hero__brand" aria-label="Apple" />
      <Pill as="span" icon="flutter" iconSize="19px" class="hero__brand" aria-label="Flutter" />
    </span>
  </div>

  <h1 class="hero__title">Mobile<br /><em class="hero__em">Developer</em><span class="hero__sup"> ／ iOS · Android</span></h1>

  <div class="hero__grid">
    <p class="hero__bio">{t.bio}</p>

    <div class="hero__socials">
      {socials.map((s) => (
        <Pill
          as="a"
          variant="card"
          icon={s.icon}
          href={s.href}
          target={s.external ? '_blank' : undefined}
          rel={s.external ? 'noopener noreferrer' : undefined}
        >
          {s.label}
        </Pill>
      ))}
    </div>

    <div class="hero__player" data-player>
      <div class="hero__scrubber">
        <div class="hero__disc" data-disc>
          <span class="hero__disc-dot"></span>
        </div>
        <div class="hero__now">
          <div class="hero__now-title">now playing — infinity_repeating.loop</div>
          <div class="hero__progress">
            <span class="hero__progress-bar" data-progress></span>
          </div>
        </div>
        <button class="hero__play" data-play-toggle aria-label={t.playAria}>
          <span class="hero__play-icon hero__play-icon--play"><Icon name="play" size="14px" /></span>
          <span class="hero__play-icon hero__play-icon--pause"><Icon name="pause" size="14px" /></span>
        </button>
      </div>
      <iframe
        class="hero__iframe"
        data-sc
        title="SoundCloud"
        width="100%"
        height="20"
        allow="autoplay"
        data-src="https://w.soundcloud.com/player/?url=https%3A%2F%2Fsoundcloud.com%2Fraven_sounddoll%2Finfinity-repeating-daft-punk-feat-julian-casablancas-the-voidz-bootleg-by-raven-sounddoll&color=%23c8ff4d&auto_play=false&hide_related=true&show_comments=false&show_user=false&show_reposts=false&show_teaser=false&visual=false"
      ></iframe>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 11vh 5vw 8vh;
    border-bottom: 1px solid var(--line);
  }

  /* Retrato */
  .hero__figure {
    margin: 0;
    position: absolute;
    top: 9vh;
    right: 5vw;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }

  /*
   * En el teléfono el 11vh de arriba se leía como un hueco muerto: la cabecera
   * no es sticky, así que su altura ya separa, y encima de ella no hay nada
   * que respirar. Se recorta a la mitad, y el retrato baja con él para no
   * despegarse del saludo.
   */
  @media (max-width: 705px) {
    .hero {
      padding-top: 5vh;
    }

    .hero__figure {
      top: 5vh;
    }
  }

  .hero__portrait-frame {
    position: relative;
    display: block;
    line-height: 0;
  }
  .hero__portrait-btn {
    display: block;
    padding: 0;
    border: 0;
    background: none;
    font: inherit;
    cursor: none;
    border-radius: 50%;
  }

  .hero__portrait {
    flex: none;
    width: clamp(96px, 13vw, 168px);
    height: clamp(96px, 13vw, 168px);
    aspect-ratio: 1;
    object-fit: cover;
    object-position: 50% 22%;
    border-radius: 50%;
    border: 1px solid var(--line);
    filter: contrast(1.05);
    transition: filter .5s ease, transform .5s ease;
    cursor: none;
  }
  /* Ver la nota de .slide-row en global.css: en táctil el :hover se queda
     pegado y el retrato se quedaba ampliado tras cada toque. */
  @media (hover: hover) and (pointer: fine) {
    .hero__portrait:hover {
      transform: scale(1.04);
    }
  }

  /* Anillo de chispas: arranca apagado, el script lo dispara por tap */
  .hero__sparks {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: clamp(96px, 13vw, 168px);
    height: clamp(96px, 13vw, 168px);
    pointer-events: none;
    z-index: 5;
  }
  .hero__spark {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 2px;
    height: 14px;
    margin: -7px 0 0 -1px;
    border-radius: 2px;
    background: var(--accent);
    opacity: 0;
    transform: rotate(var(--rot));
    transform-origin: 50% 50%;
    --a: 46px;
    --b: 74px;
  }

  /*
   * La animación se asigna solo con la clase, no se deja puesta en pausa.
   * Pausada, la primera vez corre y al terminar queda terminada: cambiar
   * animation-play-state ya no la reinicia. Quitar y volver a poner
   * animation-name sí, y es lo que hace el script en cada toque.
   */
  .hero__sparks.is-playing .hero__spark {
    animation-name: spark;
    animation-duration: 0.58s;
    animation-timing-function: cubic-bezier(0.25, 0.85, 0.35, 1);
    animation-delay: var(--delay);
    animation-fill-mode: both;
  }

  .hero__status {
    position: absolute;
    right: 2px;
    bottom: 2px;
    width: clamp(26px, 3vw, 34px);
    height: clamp(26px, 3vw, 34px);
    border-radius: 50%;
    background: var(--bg);
    border: 1px solid var(--line);
    display: grid;
    place-items: center;
    font-size: clamp(13px, 1.6vw, 17px);
    line-height: 1;
    padding-top: 1px;
    z-index: 6;
  }

  .hero__caption {
    font-family: var(--label);
    font-size: 9px;
    letter-spacing: .2em;
    text-transform: uppercase;
    color: var(--faint);
    text-align: center;
  }

  /* Saludo */
  .hero__greet {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 30px;
    max-width: min(100%, calc(95vw - 200px));
  }
  .hero__dot {
    width: 7px;
    height: 7px;
    background: var(--accent);
    border-radius: 50%;
    animation: blink 2.4s ease-in-out infinite;
    align-self: center;
  }
  .hero__greet-text {
    font-family: var(--display);
    font-size: clamp(24px, 3vw, 38px);
    letter-spacing: .02em;
    line-height: 1.1;
    color: var(--fg);
  }
  .hero__em {
    font-style: italic;
    color: var(--accent);
  }
  .hero__wave {
    font-style: normal;
  }

  /*
   * Dos copias, una por breakpoint, en vez de una que se mueva: en ancho van
   * bajo el retrato —foto, rol y plataformas como una ficha— y en estrecho el
   * retrato es un círculo pequeño arriba a la derecha donde no cabe nada
   * debajo, así que ahí siguen junto al saludo. `display: none` las saca del
   * árbol de accesibilidad, así que el lector nunca las oye dos veces.
   */
  .hero__pills {
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .hero__pills--narrow {
    display: none;
    margin-left: 4px;
  }

  /* Centradas bajo la foto, en el mismo eje que el rol. En estrecho mandan el
     saludo y el margen izquierdo. */
  .hero__pills--wide {
    margin-top: 2px;
  }

  @media (max-width: 705px) {
    .hero__pills--wide {
      display: none;
    }

    .hero__pills--narrow {
      display: inline-flex;
    }
  }

  /*
   * `.pill.hero__brand`, no `.hero__brand` a secas: Pill trae su propio
   * `background: transparent` con la misma especificidad, y el orden de los
   * bundles decidía quién ganaba. Con las dos clases no hay empate.
   *
   * Solo el logo, sin texto: el <h1> ya dice «iOS · Android» y repetirlo aquí
   * sobraba. Los tres van con la iconografía del sitio —borde tenue y el
   * color del texto— y no con el color de cada marca: son iconos de la
   * interfaz como los demás, no logos.
   *
   * Redondas y con el logo al centro: el `border-radius: 999px` de `Pill` más
   * `aspect-ratio: 1` dan el círculo, y el `gap` de `Pill` sobra porque aquí
   * solo hay icono, sin texto. El `aspect-ratio` también las iguala aunque un
   * logo sea más ancho que alto, como el de Android.
   */
  :global(.pill.hero__brand) {
    justify-content: center;
    aspect-ratio: 1;
    padding: 7px;
  }

  /* Título */
  .hero__title {
    font-family: var(--display);
    font-weight: 400;
    font-size: clamp(50px, 12vw, 148px);
    line-height: .86;
    letter-spacing: -.02em;
    margin: 0;
    text-wrap: balance;
  }

  .hero__sup {
    font-family: var(--font);
    font-size: clamp(11px, 1.1vw, 15px);
    letter-spacing: .1em;
    vertical-align: super;
    color: var(--dim);
  }

  /* Bloque inferior */
  .hero__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 32px;
    margin-top: 56px;
    align-items: end;
  }
  .hero__bio {
    margin: 0;
    font-size: 14px;
    line-height: 1.75;
    color: var(--dim);
    max-width: 44ch;
    text-wrap: pretty;
  }
  .hero__socials {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  /* Reproductor */
  .hero__player {
    position: relative;
    justify-self: end;
    width: 100%;
    max-width: 300px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .hero__scrubber {
    display: flex;
    align-items: center;
    gap: 14px;
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 9px 16px 9px 9px;
    background: var(--card);
  }
  .hero__disc {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: conic-gradient(from 0deg, var(--fg), var(--bg), var(--fg));
    position: relative;
    flex: none;
    animation: spin 6s linear infinite;
    animation-play-state: paused;
  }
  .hero__player.is-playing .hero__disc {
    animation-play-state: running;
  }
  .hero__disc-dot {
    position: absolute;
    inset: 42%;
    background: var(--accent);
    border-radius: 50%;
  }
  .hero__now {
    flex: 1;
    min-width: 0;
  }
  .hero__now-title {
    font-size: 10px;
    letter-spacing: .1em;
    color: var(--dim);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .hero__progress {
    height: 3px;
    background: var(--line);
    border-radius: 2px;
    margin-top: 7px;
    overflow: hidden;
  }
  .hero__progress-bar {
    display: block;
    height: 100%;
    width: 0;
    background: var(--accent);
    transition: width .25s linear;
  }
  .hero__play {
    cursor: none;
    border: none;
    background: none;
    color: var(--fg);
    font: inherit;
    padding: 0;
    display: flex;
    align-items: center;
  }
  .hero__play:hover {
    color: var(--accent);
  }
  .hero__play-icon {
    display: block;
  }
  .hero__play-icon--pause {
    display: none;
  }
  .hero__player.is-playing .hero__play-icon--play {
    display: none;
  }
  .hero__player.is-playing .hero__play-icon--pause {
    display: block;
  }
  /*
   * El iframe es el motor del widget, no una interfaz: el reproductor visible
   * es el de arriba. Se saca del flujo y se vuelve invisible, pero se deja
   * renderizado y con tamaño real. Ni display:none ni visibility:hidden:
   * ambos pueden impedir que el widget cargue o reproduzca.
   */
  .hero__iframe {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 20px;
    /* Reemplaza al frameborder="no" de HTML4, que además está deprecado. */
    border: 0;
    opacity: 0;
    pointer-events: none;
  }
</style>
