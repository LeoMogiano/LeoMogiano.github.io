// VARIABLES

// Let es para variables y const es para constantes

let resultado;
resultado = 3.9;
//resultado = Math.round(resultado) //redondear
//resultado = Math.ceil(resultado) // redondear hacia arriba
resultado = Math.floor(resultado) //hacia abajo

console.log(resultado);
