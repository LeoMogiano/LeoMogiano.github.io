{
  "name": "leomogiano-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "assets": "node scripts/extract-assets.mjs && node scripts/prepare-assets.mjs",
    "fonts": "node scripts/subset-fonts.mjs",
    "test": "astro build && node --test 'tests/*.test.mjs'"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^26.4.0",
    "sharp": "^0.35.4",
    "subset-font": "^2.4.0",
    "typescript": "^6.0.3"
  }
}
